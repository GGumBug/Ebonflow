public enum TeamType
{
    Ally,
    Enemy
}

public enum UnitState
{
    Idle,
    Walk,
    Attack,
}

public enum StatType
{
    Hp,
    Attack,
    AttackDelay,
    Range,
}

public enum ModifierMode
{
    Add, 
    Mul,
}
